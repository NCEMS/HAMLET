name: cascadia_env
channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - pip:
      - torch
      - torchvision
      - torchaudio
      - cascadia
      - psims>=1.3.6
      - setuptools<82
